const boxProps = {
  flex: 1,
  justifyContent: 'flex-start',
  padding: 1,
  display: 'flex',
};

const buttonFullWidthProps = {
  width: '100%',
  marginY: 1,
};

const buttonNonFullWidthProps = {
  marginX: 0.5,
  padding: 2,
};

export { boxProps, buttonFullWidthProps, buttonNonFullWidthProps };
